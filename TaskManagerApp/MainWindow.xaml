<Window x:Class="TaskManagerApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskManagerApp"
        mc:Ignorable="d"
        Title="Менеджер Задач" Height="600" Width="1000"
        Background="White"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <local:TaskStatusConverter x:Key="StatusConverter"/>
        <local:TaskPriorityConverter x:Key="PriorityConverter"/>
        
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#999999"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D0D0D0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#999999"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="ComboStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#999999"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="GridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#999999"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F5F5F5"/>
            <Setter Property="GridLinesVisibility" Value="All"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Загрузить JSON" Style="{StaticResource ButtonStyle}" Click="LoadJsonButton_Click"/>
            <Button Content="Сохранить JSON" Style="{StaticResource ButtonStyle}" Click="SaveJsonButton_Click"/>
            <Button Content="Загрузить XML" Style="{StaticResource ButtonStyle}" Click="LoadXmlButton_Click"/>
            <Button Content="Сохранить XML" Style="{StaticResource ButtonStyle}" Click="SaveXmlButton_Click"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="150"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Статус:" Foreground="Black" VerticalAlignment="Center" Margin="5"/>
            <ComboBox x:Name="StatusFilterComboBox" Grid.Column="1" Style="{StaticResource ComboStyle}" 
                      SelectionChanged="StatusFilterComboBox_SelectionChanged"/>

            <TextBlock Grid.Column="2" Text="Поиск:" Foreground="Black" VerticalAlignment="Center" Margin="15,5,5,5"/>
            <TextBox x:Name="SearchTextBox" Grid.Column="3" Style="{StaticResource TextBoxStyle}" 
                     TextChanged="SearchTextBox_TextChanged"/>

            <TextBlock Grid.Column="4" Text="Сортировка:" Foreground="Black" VerticalAlignment="Center" Margin="15,5,5,5"/>
            <ComboBox x:Name="SortComboBox" Grid.Column="5" Style="{StaticResource ComboStyle}" 
                      SelectionChanged="SortComboBox_SelectionChanged"/>
        </Grid>

        <DataGrid x:Name="TasksDataGrid" Grid.Row="2" Style="{StaticResource GridStyle}"
                  SelectionChanged="TasksDataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Title}" Width="*" MinWidth="150"/>
                <DataGridTextColumn Header="Приоритет" Binding="{Binding Priority, Converter={StaticResource PriorityConverter}}" Width="100"/>
                <DataGridTextColumn Header="Срок" Binding="{Binding DueDate, StringFormat={}{0:dd.MM.yyyy}}" Width="100"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status, Converter={StaticResource StatusConverter}}" Width="120"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*" MinWidth="200"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="Добавить задачу" Style="{StaticResource ButtonStyle}" Click="AddTaskButton_Click"/>
            <Button Content="Редактировать" Style="{StaticResource ButtonStyle}" Click="EditTaskButton_Click"/>
            <Button Content="Удалить" Style="{StaticResource ButtonStyle}" Click="DeleteTaskButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
